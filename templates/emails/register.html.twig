<h1>Demande d'autorisation d'accès aux données</h1>
<p>Bonjour
	{{ franchise.name }},</p>
<p>Votre inscription sur le site FJ Fitness est à valider en cliquant sur le lien ci-dessous :</p>
<p></p>
<p>
	<a href="{{ absolute_url(path('verify_user', {token: token})) }}">{{ absolute_url(path('verify_user', {token: token})) }}</a>
</p></p><p>Ce lien expirera dans 3 heures</p>
