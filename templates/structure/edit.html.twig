{% extends 'admin.html.twig' %}

{% block body %}


	<div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
		<h2>Modification de la structure :
			{{ structure.name }}</h2>
		<div class="btn-toolbar mb-2 mb-md-0">
			<div class="btn-group me-2">
				<a href="{{ path('app_dashboard_structure_index') }}" role="button" class="btn btn-outline-success mt-2">Retour à la liste</a>

				{{ include('structure/_delete_form.html.twig') }}
			</div>
		</div>
	</div>
	{{ include('structure/_edit_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}
