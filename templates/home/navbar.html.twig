<nav class="navbar navbar-expand-lg navbar-light fixed-top bg-light" id="mainNav">
	<div class="container">
		<h1>
			<a class="navbar-brand text-uppercase fw-bold" href="{{ path('app_home')}}">FJ Fitness</a>
		</h1>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarSupportedContent">
			<ul class="navbar-nav mx-auto">
				<li class="nav-item px-lg-4">
					<a class="nav-link text-uppercase active" href="{{ path('app_home')}}">Accueil</a>
				</li>
				<!--<li class="nav-item px-lg-4">
									<a class="nav-link text-uppercase" href="evenements.html">&Eacute;vénements</a>
								</li>-->
			</ul>

			{% if app.user %}
				<span class="navbar-text">
					Bonjour,
					<a href="/dashboard/admin">{{ app.user.name }}</a>
					&nbsp;
					<a class="btn btn-outline-success" href="{{ path('app_logout') }}">Se déconnecter</a>
				</span>
			{% else %}
				<span class="navbar-text">
					<a class="btn btn-outline-success" href="{{ path('app_login') }}">Se connecter</a>
				</span>
			{% endif %}
		</div>
	</div>
</nav>
